<h1><%= @ganre.name %></h1>
<%= link_to "Edit Ganre", edit_ganre_path(@ganre), class:"edit" %>
<%= link_to "Delete Ganre", ganre_path(@ganre), method: :delete,
                                                          class:"delete",
      data:{confirm:"Are you sure that you want to delete this network?"} %>
