<% title(@genre.name) %>
<h1><%= @genre.name %></h1>
<%= link_to "Edit Genre", edit_genre_path(@genre), class:"edit" %>
<%= link_to "Delete Genre", genre_path(@genre), method: :delete,
                                                          class:"delete",
      data:{confirm:"Are you sure that you want to delete this genre?"} %>
