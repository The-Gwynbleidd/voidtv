<% title(@tag.name) %>
<%= @tag.name %>
<%= link_to "Edit Tag", edit_admin_tag_path(@tag), class:"edit" %>
<%= link_to "Delete Tag", admin_tag_path(@tag), method: :delete,
                                                          class:"delete",
      data:{confirm:"Are you sure that you want to delete this tag?"} %>
